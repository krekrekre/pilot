import VisualPerceptionModule from '@/components/training/VisualPerceptionModule';

export const metadata = { title: 'Visual Perception – Pilot Prep' };

export default function VisualPerceptionPage() {
  return <VisualPerceptionModule />;
}
