import PasswordModule from '@/components/training/PasswordModule';

export const metadata = { title: 'Symbol Pattern (PASSWORD) – Pilot Prep' };

export default function PasswordPage() {
  return <PasswordModule />;
}
