import ClockModule from '@/components/training/ClockModule';

export const metadata = { title: 'Spatial Orientation (CLOCK) – Pilot Prep' };

export default function ClockPage() {
  return <ClockModule />;
}
