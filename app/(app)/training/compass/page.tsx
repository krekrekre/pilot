import CompassModule from '@/components/training/CompassModule';

export const metadata = { title: 'Spatial Orientation (COMPASS) – Pilot Prep' };

export default function CompassPage() {
  return <CompassModule />;
}
