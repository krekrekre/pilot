import FocusModule from '@/components/training/FocusModule';

export const metadata = { title: 'Focus Maintaining (RULES) – Pilot Prep' };

export default function FocusPage() {
  return <FocusModule />;
}
